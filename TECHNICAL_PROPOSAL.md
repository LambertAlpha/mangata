# Mangata - 去中心化内容订阅平台技术提案

> **项目名称**: Mangata (他加禄语：月光在水面的倒影)
> **黑客松**: Walrus Haulout Hackathon 2025
> **赛道**: 数据安全与隐私 (Data Security & Privacy)
> **提案日期**: 2025年11月21日
> **开发周期**: 3天 (11月21日 - 11月23日)

---

## 1. 项目概述

Mangata是一个基于Sui区块链的去中心化内容订阅平台，通过NFT门控机制实现创作者内容的加密分发和变现。平台利用Walrus去中心化存储和Seal加密协议，确保内容在存储和传输过程中的隐私性和安全性。

**核心价值主张**:
- **真正的隐私保护**: 内容在客户端加密，Walrus上仅存储密文，连平台方都无法访问原文
- **抗审查**: 去中心化存储确保内容不会被任意删除或篡改
- **创作者主权**: 100%收益归创作者，平台不抽成（仅支付链上gas费）
- **透明可验证**: 所有交易和访问控制逻辑在链上公开可审计

---

## 2. 问题陈述

### 2.1 现有内容平台的痛点

**中心化平台（Patreon, OnlyFans等）**:
- ❌ 平台抽成高达10-30%
- ❌ 内容存储在中心化服务器，存在审查风险
- ❌ 创作者无法真正"拥有"自己的内容和用户关系
- ❌ 用户隐私数据被平台掌控和贩卖

**Web3现有方案的不足**:
- ❌ 大部分NFT平台仅做"所有权证明"，内容本身未加密
- ❌ IPFS等存储方案虽去中心化，但内容公开可访问
- ❌ 缺乏细粒度的访问控制机制

### 2.2 Mangata的解决思路

通过结合**Sui区块链的高性能**、**Walrus的低成本存储**和**Seal的密码学访问控制**，我们实现：
1. 内容加密存储 → 只有授权用户能解密
2. NFT作为访问凭证 → 链上验证，无需信任中介
3. 可转让的访问权限 → NFT二级市场流通，创作者持续获得版税

---

## 3. 技术架构

### 3.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面层                           │
│  ┌──────────────┐         ┌──────────────┐                  │
│  │ 创作者控制台  │         │ 内容市场     │                  │
│  │ - 内容上传   │         │ - NFT浏览    │                  │
│  │ - NFT铸造    │         │ - 购买解锁   │                  │
│  └──────────────┘         └──────────────┘                  │
└─────────────────────────────────────────────────────────────┘
              │                         │
              ▼                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      应用逻辑层 (TypeScript)                 │
│  ┌────────────┐  ┌─────────────┐  ┌────────────┐            │
│  │ Sui SDK    │  │ Seal SDK    │  │ Walrus SDK │            │
│  │ - 钱包连接  │  │ - 加密/解密  │  │ - 上传下载 │            │
│  │ - 合约调用  │  │ - 密钥管理   │  │ - Blob管理 │            │
│  └────────────┘  └─────────────┘  └────────────┘            │
└─────────────────────────────────────────────────────────────┘
              │                │                 │
              ▼                ▼                 ▼
┌──────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Sui 区块链       │  │ Seal密钥服务器   │  │ Walrus存储网络  │
│                  │  │                 │  │                 │
│ Move智能合约:     │  │ - 阈值解密      │  │ - 纠删码存储    │
│ • ContentNFT     │  │ - 访问验证      │  │ - 高可用性      │
│ • AccessControl  │  │ - IBE密钥派发   │  │ - 低成本        │
│ • MarketPlace    │  │                 │  │                 │
└──────────────────┘  └─────────────────┘  └─────────────────┘
```

### 3.2 数据流设计

#### 创作者上传内容流程
```
1. 创作者选择文件 (图片/文本/视频)
   ↓
2. 前端生成随机对称密钥 AES-256
   ↓
3. 客户端加密内容 → ciphertext
   ↓
4. 上传ciphertext到Walrus → 获得blob_id
   ↓
5. 使用Seal加密对称密钥 → encrypted_key
   (访问策略: 绑定到即将铸造的NFT合约)
   ↓
6. 调用Sui Move合约 mint_content_nft()
   - 参数: blob_id, encrypted_key, price, preview_image
   ↓
7. NFT铸造成功 → 创作者持有ContentNFT对象
```

#### 用户购买并访问内容流程
```
1. 用户浏览市场，看到加密内容的预览
   ↓
2. 点击"购买" → 调用 purchase_nft()
   - 支付SUI代币
   - NFT所有权转移到用户地址
   ↓
3. 前端检测到用户持有NFT
   ↓
4. 从NFT对象中读取 blob_id 和 encrypted_key
   ↓
5. 使用Seal SDK解密对称密钥
   - 提供NFT持有证明 (ownership proof)
   - Seal验证访问策略 → 返回decrypted_key
   ↓
6. 从Walrus下载ciphertext (通过blob_id)
   ↓
7. 使用decrypted_key解密内容
   ↓
8. 在浏览器中渲染完整内容
```

---

## 4. 核心功能模块

### 4.1 智能合约层 (Sui Move)

#### Module 1: `content_nft.move`

**核心数据结构**:
```move
struct ContentNFT has key, store {
    id: UID,
    /// Walrus存储的加密内容blob ID
    blob_id: String,
    /// Seal加密的对称密钥
    encrypted_key: vector<u8>,
    /// 创作者地址
    creator: address,
    /// 售价 (SUI)
    price: u64,
    /// 预览图URL (未加密，公开可访问)
    preview_url: String,
    /// 内容元数据
    title: String,
    description: String,
    content_type: String, // "image" | "text" | "video"
    /// 创建时间戳
    created_at: u64,
}

/// 创作者全局注册表
struct CreatorRegistry has key {
    id: UID,
    creators: Table<address, vector<ID>>, // creator -> [nft_ids]
}

/// 市场配置
struct MarketConfig has key {
    id: UID,
    /// 平台版税百分比 (基点, 1% = 100)
    royalty_bps: u64,
    /// 版税接收地址
    royalty_recipient: address,
}
```

**关键函数**:
```move
/// 铸造内容NFT
public fun mint_content_nft(
    blob_id: String,
    encrypted_key: vector<u8>,
    price: u64,
    preview_url: String,
    title: String,
    description: String,
    content_type: String,
    ctx: &mut TxContext
): ContentNFT

/// 购买NFT (一次性买断)
public fun purchase_nft(
    nft: ContentNFT,
    payment: Coin<SUI>,
    config: &MarketConfig,
    ctx: &mut TxContext
)

/// 创作者更新价格
public fun update_price(
    nft: &mut ContentNFT,
    new_price: u64,
    ctx: &TxContext
)

/// 转让NFT (支持二级市场)
public fun transfer_nft(
    nft: ContentNFT,
    recipient: address,
    ctx: &TxContext
)
```

#### Module 2: `access_control.move`

**Seal集成模块**:
```move
/// 验证用户是否有权限访问内容
public fun verify_access(
    user: address,
    nft_id: ID,
    ctx: &TxContext
): bool {
    // 检查user是否持有nft_id对应的NFT
    // 此函数由Seal SDK在解密前调用
}

/// 生成访问证明 (用于Seal解密)
public fun generate_access_proof(
    nft: &ContentNFT,
    ctx: &TxContext
): vector<u8> {
    // 返回NFT所有权的密码学证明
    // Seal服务器验证此证明后才会协助解密
}
```

### 4.2 前端应用层 (TypeScript + React)

#### 技术栈选型
- **框架**: Next.js 14 (App Router)
- **UI库**: Tailwind CSS + shadcn/ui
- **钱包集成**: @mysten/dapp-kit
- **状态管理**: Zustand
- **加密库**: crypto-js (AES-256)
- **文件上传**: react-dropzone

#### 关键组件

**1. 创作者控制台 (`/create`)**
```typescript
// 核心上传逻辑
async function uploadContent(file: File, metadata: ContentMetadata) {
  // 1. 生成对称密钥
  const symmetricKey = CryptoJS.lib.WordArray.random(256/8)

  // 2. 加密文件
  const encrypted = CryptoJS.AES.encrypt(
    await file.arrayBuffer(),
    symmetricKey
  )

  // 3. 上传到Walrus
  const blobId = await walrusClient.upload(encrypted.toString())

  // 4. Seal加密对称密钥
  const sealClient = new SealClient(sealConfig)
  const encryptedKey = await sealClient.encrypt(
    symmetricKey.toString(),
    { policy: `nft_contract::${packageId}` }
  )

  // 5. 铸造NFT
  const tx = new TransactionBlock()
  tx.moveCall({
    target: `${packageId}::content_nft::mint_content_nft`,
    arguments: [
      tx.pure(blobId),
      tx.pure(encryptedKey),
      tx.pure(metadata.price),
      tx.pure(metadata.previewUrl),
      tx.pure(metadata.title),
      tx.pure(metadata.description),
      tx.pure(metadata.contentType),
    ]
  })

  return await signAndExecuteTransactionBlock({ transactionBlock: tx })
}
```

**2. 内容市场 (`/marketplace`)**
- NFT卡片网格展示
- 过滤器 (按类型、价格、创作者)
- 搜索功能
- 购买模态框

**3. 内容查看器 (`/view/[nftId]`)**
```typescript
async function decryptAndView(nftId: string, userAddress: string) {
  // 1. 验证用户持有NFT
  const nft = await suiClient.getObject({ id: nftId })
  if (nft.owner !== userAddress) {
    throw new Error("Unauthorized: You don't own this NFT")
  }

  // 2. 提取加密密钥和blob_id
  const { blob_id, encrypted_key } = nft.content.fields

  // 3. 使用Seal解密对称密钥
  const sealClient = new SealClient(sealConfig)
  const decryptedKey = await sealClient.decrypt(encrypted_key, {
    proof: await generateOwnershipProof(nftId, userAddress)
  })

  // 4. 从Walrus下载加密内容
  const encryptedContent = await walrusClient.download(blob_id)

  // 5. 解密内容
  const content = CryptoJS.AES.decrypt(
    encryptedContent,
    CryptoJS.enc.Hex.parse(decryptedKey)
  )

  // 6. 渲染内容
  return content
}
```

---

## 5. 技术栈总览

### 5.1 区块链层
| 组件 | 技术选型 | 版本 | 用途 |
|------|---------|------|------|
| 智能合约 | Sui Move | 1.0 | NFT逻辑、访问控制 |
| 区块链节点 | Sui Testnet | - | 合约部署和交易执行 |
| 钱包 | Sui Wallet | - | 用户身份认证 |

### 5.2 存储和加密层
| 组件 | 技术选型 | 版本 | 用途 |
|------|---------|------|------|
| 去中心化存储 | Walrus | Latest | 加密内容存储 |
| 加密协议 | Seal | Latest | 密钥管理和访问控制 |
| 对称加密 | AES-256-GCM | - | 客户端内容加密 |

### 5.3 前端应用层
| 组件 | 技术选型 | 版本 | 用途 |
|------|---------|------|------|
| 框架 | Next.js | 14.2 | React全栈框架 |
| UI库 | Tailwind CSS | 3.4 | 样式框架 |
| 组件库 | shadcn/ui | Latest | UI组件 |
| Sui集成 | @mysten/sui.js | 0.54 | 链交互 |
| Sui钱包 | @mysten/dapp-kit | Latest | 钱包连接 |
| Seal SDK | @mysten/seal | Latest | 加密解密 |
| 状态管理 | Zustand | 4.5 | 全局状态 |

---

## 6. MVP功能范围 (3天可交付)

### 6.1 必须实现 (P0)
- ✅ 创作者上传图片内容 (仅支持图片，<10MB)
- ✅ 客户端AES加密 + Walrus存储
- ✅ Seal加密对称密钥
- ✅ Move合约铸造ContentNFT
- ✅ 市场展示NFT列表 (含未加密预览图)
- ✅ 用户购买NFT (SUI支付)
- ✅ NFT持有者解密查看完整内容
- ✅ 基础UI (响应式布局)

### 6.2 可选实现 (P1)
- 🔶 创作者个人主页
- 🔶 NFT转售市场 (二级交易)
- 🔶 版税机制 (创作者从二次销售获得10%分成)
- 🔶 支持文本内容 (Markdown)

### 6.3 明确不做 (Out of Scope)
- ❌ 视频内容 (文件太大，解密慢)
- ❌ 订阅制 (需要复杂的时间锁和keeper)
- ❌ 社交功能 (评论、点赞、关注)
- ❌ 推荐算法
- ❌ 移动端App
- ❌ 多链支持

---

## 7. 实施路线图

### Day 1 (11月21日): 基础设施搭建
**上午 (4小时)**
- [ ] 环境准备
  - [ ] 安装Sui CLI，配置测试网
  - [ ] Clone Seal和Walrus repos
  - [ ] 跑通Seal SDK examples
  - [ ] 测试Walrus上传下载
- [ ] Discord确认密钥服务器可用性

**下午 (4小时)**
- [ ] Move合约开发
  - [ ] 编写`content_nft.move` 核心逻辑
  - [ ] 编写`access_control.move`
  - [ ] 本地测试和部署到Sui testnet
  - [ ] 验证合约调用成功

**晚上 (2小时)**
- [ ] 前端项目初始化
  - [ ] Next.js脚手架搭建
  - [ ] 安装依赖 (Sui SDK, Seal SDK, Tailwind)
  - [ ] 配置钱包连接

**Day 1 检查点**: Move合约部署成功，前端能连接钱包并调用合约

---

### Day 2 (11月22日): 核心功能实现
**上午 (5小时)**
- [ ] 创作者上传功能
  - [ ] 文件上传UI (react-dropzone)
  - [ ] 集成AES加密逻辑
  - [ ] 集成Walrus上传
  - [ ] 集成Seal加密
  - [ ] 调用Move合约铸造NFT
  - [ ] 测试完整上传流程

**下午 (5小时)**
- [ ] 市场和查看功能
  - [ ] 查询链上NFT列表
  - [ ] 渲染市场卡片UI
  - [ ] 实现购买功能
  - [ ] NFT持有验证
  - [ ] Seal解密逻辑
  - [ ] Walrus下载逻辑
  - [ ] AES解密并渲染内容

**晚上 (2小时)**
- [ ] 端到端测试
  - [ ] 创作者上传 → 用户购买 → 查看内容
  - [ ] 修复关键bug

**Day 2 检查点**: 完整的创作-购买-查看流程打通

---

### Day 3 (11月23日): 优化和提交
**上午 (3小时)**
- [ ] UI美化
  - [ ] 优化市场页布局
  - [ ] 添加加载状态和错误处理
  - [ ] 响应式设计调整
  - [ ] 添加预览图占位符

**下午 (4小时)**
- [ ] Demo准备
  - [ ] 录制演示视频 (3-5分钟)
    - [ ] 展示创作者上传流程
    - [ ] 展示用户购买流程
    - [ ] 展示内容解密查看
    - [ ] 强调隐私保护特性
  - [ ] 撰写项目README
  - [ ] 准备技术文档
  - [ ] 部署到Vercel (如有时间)

**晚上 (1小时)**
- [ ] 最终检查
  - [ ] 验证所有提交材料
  - [ ] 提交到DeepSurge平台
  - [ ] 备份代码

**Day 3 检查点**: 提交成功，Demo视频清晰展示核心价值

---

## 8. 风险分析与缓解措施

### 8.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| Seal密钥服务器测试网不稳定 | 🔴 高 | 中 | **Day 1上午立即验证**，如不可用考虑自建或降级到纯Walrus方案 |
| Walrus上传速度慢 | 🟡 中 | 中 | 限制文件大小<10MB，显示上传进度条，超时重试机制 |
| Move合约bug导致资金损失 | 🔴 高 | 低 | 充分测试，使用Sui testnet代币，添加紧急暂停功能 |
| Seal解密失败率高 | 🟡 中 | 中 | 添加重试逻辑，记录详细错误日志，准备降级方案 |
| 前端加密性能问题 | 🟡 中 | 低 | 使用Web Workers处理加密，避免阻塞主线程 |

### 8.2 时间风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 环境搭建耗时超预期 | 🔴 高 | 高 | **立即开始**，遇到问题马上Discord求助，准备备用方案 |
| Seal SDK文档不清晰 | 🟡 中 | 中 | 直接阅读源码和examples，Discord联系官方团队 |
| 集成调试时间不足 | 🔴 高 | 中 | **优先实现核心功能**，砍掉所有P1特性，确保P0功能完整 |

### 8.3 竞争风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 其他团队做类似项目 | 🟡 中 | 高 | **强调差异化**：强调端到端加密、创作者主权、可转让访问权 |
| 评委不理解技术价值 | 🟡 中 | 中 | Demo视频简化技术术语，**讲故事而非讲技术**，对比Patreon痛点 |

---

## 9. 成功标准

### 9.1 技术标准 (必须满足)
- ✅ 创作者能成功上传并铸造NFT
- ✅ 用户能成功购买NFT
- ✅ NFT持有者能解密并查看完整内容
- ✅ 非持有者无法访问加密内容
- ✅ Walrus上存储的内容确实是密文 (可验证)
- ✅ 没有严重安全漏洞 (如权限绕过)

### 9.2 产品标准 (尽力而为)
- 🎯 UI设计简洁美观，不掉档次
- 🎯 操作流程流畅，无明显卡顿
- 🎯 错误提示友好，用户能理解
- 🎯 Demo视频专业，3分钟内讲清楚核心价值

### 9.3 黑客松标准 (争取加分)
- 🏆 项目完整度 >80% (不是半成品)
- 🏆 技术创新性 (Seal + Walrus组合使用)
- 🏆 实用性 (真实解决创作者变现痛点)
- 🏆 代码质量 (结构清晰，有注释)
- 🏆 文档完整 (README + 架构图)

---

## 10. 关键决策记录

### 决策1: 为什么选择NFT门控而非订阅制？
**理由**:
- 订阅需要定期扣款和到期检查，Sui没有原生cron job
- 3天时间实现链下keeper不现实
- NFT模式更简单，逻辑更清晰，评委更容易理解

### 决策2: 为什么只支持图片？
**理由**:
- 视频文件太大 (>100MB)，Walrus上传和下载慢
- 解密大文件需要时间，Demo演示时会很尴尬
- 图片已经足够证明技术可行性

### 决策3: 为什么不做社交功能？
**理由**:
- 社交功能 (评论、点赞) 需要额外的后端服务和数据库
- 与黑客松赛道主题 (数据安全与隐私) 关系不大
- 时间不允许

### 决策4: 为什么不部署主网？
**理由**:
- 测试网足够展示功能
- 主网部署需要真实SUI代币，有资金风险
- Seal和Walrus主网可能尚未正式上线

---

## 11. 参考资料

### 官方文档
- Sui开发文档: https://docs.sui.io/
- Walrus文档: https://docs.wal.app/
- Seal文档: https://seal-docs.wal.app/
- Seal GitHub: https://github.com/MystenLabs/seal
- Nautilus文档: https://docs.sui.io/concepts/cryptography/nautilus

### 黑客松资源
- Haulout官网: https://www.walrus.xyz/haulout
- DeepSurge平台: (提交入口)
- Sui Discord: https://discord.gg/sui
- Walrus Discord频道: (技术支持)

### 技术参考
- Sui Move示例: https://github.com/MystenLabs/sui/tree/main/examples
- Seal TypeScript SDK: https://www.npmjs.com/package/@mysten/seal
- Sui dApp Kit: https://sdk.mystenlabs.com/dapp-kit

---

## 12. 附录: 备用方案

### 如果Seal不可用怎么办？

**降级方案A: 纯Walrus + 客户端加密**
- 对称密钥不用Seal加密，直接存在NFT metadata中
- 仅NFT持有者的前端能读取metadata并解密
- 缺点: 密钥暴露在链上 (但普通用户看不懂)

**降级方案B: 改用Sui Kiosk + 私有metadata**
- 使用Sui官方的Kiosk标准
- metadata存储在链下服务器，API验证NFT持有权后返回
- 缺点: 引入中心化服务器，违背去中心化初衷

**推荐**: 如果Day 1上午发现Seal不可用，**立即切换到降级方案A**，不要浪费时间死磕。

---

## 13. 团队与联系方式

**开发者**: Lambert Lin
**技能栈**: Move, TypeScript, React, Sui生态
**GitHub**: (待补充)
**Discord**: (待补充)
**Twitter**: (待补充)

---

**最后更新**: 2025年11月21日
**文档版本**: v1.0
**状态**: 待执行 ⏳
